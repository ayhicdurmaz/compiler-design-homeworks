package main

func main() {
    // Simple declarations
    x := 42
    y := true
    z := "hello world"
    
    // Arithmetic expressions
    result := (x + 10) * 2 - 5
    
    // Logical expressions
    flag := x > 0 && y
    
    // Nested if-else
    if x > 50 {
        result = 100
    } else if x > 30 {
        result = 50
    } else {
        result = 0
    }
}
